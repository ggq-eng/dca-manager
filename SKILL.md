---
name: dca-manager
description: 复利定投管理系统（DCA Manager）——包含可部署的单文件定投管理网页应用、A股实时行情代理、每日定投监控日报生成器与策略回归测试。当用户需要创建/部署复利定投管理程序、为某只股票生成每日定投监控日报（档位判定、止损/止盈点位、风险提示）、配置区间定投策略（上限暂停/区间正常/下限加码）、或对定投策略引擎做回归验证时使用。触发词：复利定投、定投管理、定投日报、每日定投监控、DCA、定投策略回测。
agent_created: true
---

# 复利定投管理系统（DCA Manager）

## Overview

提供一个开箱即用的**复利定投工作台**：单文件网页应用（无依赖、可离线、localStorage 持久化）负责策略配置、GBM 模拟回测、实时诊断；Python 脚本负责行情获取与每日监控日报生成；JS 测试脚本保证策略引擎回归正确。

## 核心能力

### 1. 部署定投管理程序（网页应用）

1. 将 `assets/dca-manager.html` 复制到用户工作区（或让用户直接双击打开）。单文件、零依赖、深色主题、涨红跌绿。
2. 启动本地行情代理（解决浏览器直连腾讯行情被 CORS 拦截的问题）：
   ```bash
   python scripts/quote_proxy.py 8000
   ```
   验证：`curl "http://localhost:8000/quote?code=sh600000"` 应返回 JSON。
3. 网页能力：
   - **配置面板**：账户（本金/单期金额/日周月频率）、标的与触发区间（上限暂停/区间正常/下限加码×倍数）、风控（固定止盈/移动止盈/止损/卖出比例/复利开关）、模拟参数（GBM：时长/收益/波动/种子/无风险利率）、行情接口
   - **运行模拟**：逐帧动画回放 GBM 价格序列，输出资产曲线、买卖点、摘要卡（总资产/盈亏/年化/最大回撤/夏普/胜率）、交易记录表 + CSV 导出
   - **当前价位诊断**：输入价格实时判定档位、浮盈、距止盈/止损线距离，输出操作建议
   - **行情刷新**：自动走「本地代理 → 腾讯直连(GBK)」两级 fallback

### 2. 生成每日定投监控日报

运行 `scripts/generate_report.py`，一键抓取真实行情并输出 Markdown 日报：

```bash
python scripts/generate_report.py --code sh600000 --name 浦发银行
# 全部参数可自定义，默认与网页配置一致（见 references/strategy.md）
```

脚本行为：
- **行情三级 fallback**：本地代理（http://localhost:8000/quote）→ 腾讯直连（qt.gtimg.cn，GBK 解码）→ 均失败则非零退出。
- **WebSearch 兜底**：若脚本失败，由 AI 用 WebSearch 搜索该股票当日最新收盘价，标注数据来源为搜索结果，继续生成日报。
- 输出文件：`--out 指定目录/YYYY-MM-DD.md`（默认 `./日报/`，不存在则创建）。
- 日报包含：行情快照、定投档位判定、止损/止盈点位表（含距现价百分比）、风险提示（距止损<3% 红警、距止盈<3% 黄示、加码区交互说明）、明日操作建议。

### 3. 策略引擎回归测试

```bash
node scripts/test_engine.js
```
7 个用例覆盖：区间暂停 / 加码买入 / 固定止盈 / 止损 / 复利累计投入>本金 / 移动止盈 / GBM 可复现。改动策略逻辑后必须跑通。

## 工作流：日常使用

**场景 A —— 用户要看今天的定投日报**：
1. 运行 `generate_report.py`（先探测本地代理，未启动则后台拉起 `quote_proxy.py 8000` 后重试）。
2. 行情成功 → 直接采用脚本输出；失败 → WebSearch 兜底并在日报注明"行情获取失败，数据为最近一次已知值"及失败原因。
3. 回复用户简洁版日报（8~15 行）：今日价格与涨跌幅、定投档位与本期应投金额、止损/止盈点价格、距止损/止盈距离、1 条操作建议。
4. 用 present_files 呈现生成的日报文件。

**场景 B —— 用户要新建/自定义定投计划**：
1. 先询问：标的代码、本金、单期金额、频率、区间上下限、加码倍数、止盈/止损比例、是否复利再投入、是否启用移动止盈。
2. 按用户参数运行 `generate_report.py` 或指导在网页配置面板中填写（参数语义见 `references/strategy.md`）。

**场景 C —— 用户要分享/打包本系统**：整个 skill 目录即分发包，见下方「分享给他人」。

## 关键规则（务必遵守）

- **档位判定**（以现价 price、上限 upper、下限 lower 为准）：
  - `price > upper` → 暂停本期（投入 0）
  - `price < lower` → 加码买入（单期金额 × 加码倍数）
  - `price ∈ [lower, upper]` → 正常买入（单期金额）
- **风控点位**：止损价 = 参考成本 × (1 − 止损%)；止盈价 = 参考成本 × (1 + 止盈%)。参考成本优先取持仓摊薄成本；无持仓数据时取现价并在日报注明基准。
- **移动止盈**：浮盈 ≥ 启动阈值时激活，自持仓最高价回撤 ≥ 回撤阈值时触发卖出。
- **加码区交互**（重要风险点）：若止损价 < 加码下限，则价格跌至「加码下限 ~ 止损价」区间时**仍会加码买入**（越跌越买），跌破止损价才出清——日报中必须提示该风险敞口。
- **合规**：日报仅为监控参考，不执行任何真实交易；不构成投资建议，日报末尾需附免责声明。
- **货币与颜色**：金额用 ¥；A 股语境涨红跌绿。

## 资源说明

| 资源 | 用途 |
|---|---|
| `assets/dca-manager.html` | 定投管理网页应用（单文件，复制即用） |
| `scripts/quote_proxy.py` | 本地行情代理（腾讯 qt.gtimg.cn 转发 + CORS，纯标准库） |
| `scripts/generate_report.py` | 每日监控日报生成器（行情抓取 + 档位计算 + Markdown 输出） |
| `scripts/test_engine.js` | 策略引擎回归测试（7 用例） |
| `references/strategy.md` | 策略参数语义、默认值、腾讯行情字段格式、风险交互详解 |

## 分享给他人

- 分发 `dca-manager.zip`（由 package_skill.py 生成），接收方解压到 `~/.workbuddy/skills/dca-manager/` 即可被 WorkBuddy 识别。
- 也可将 `assets/dca-manager.html` 单独发出（双击即用，行情刷新需本机运行 quote_proxy.py）。
